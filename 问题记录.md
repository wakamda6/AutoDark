# 屏幕点击器

### 屏幕点击器PWM控制失败
经验证，esp32不能满足pwm频率为5hz的输出脉冲，因此改为推挽输出控制3.3V（5V更稳定）

### 充电时很不稳定
补充点，亮度调到最低，可以至少12小时待机

### 和屏幕贴合不牢固，时间长了会掉
等待更换模块。找不到合适的模块的情况下，先使用autodingding

 > 经测试，物理屏幕点击头方案，由于硬件限制（电压达不到稳定要求，无法稳定实现功能），暂时放弃硬件方案。

# AutoDark项目

需要对AutoDark进行定期更新。

## 测试

程序进行测试时，使用其他手机进行测试，安装钉钉和autodark，测试一周以上。

## 2024.12.10

目前现状：

stable_1.5.5版本，是目前正在使用的稳定版本。稳定使用时间10天以上。

但是有一些异常问题必须解决：

1. 解决mqtt经常服务停止的问题( 添加重连机制，测试中 )

2. log优化( 完成 )

3. 通知消息发送大于1次。(未解决)

   怀疑是通知监听器问题，可借鉴develop分支相关代码

4. mqtt client id问题( 完成 )
 
   要根据相关命名规则，重新设置id，用户名和密码

5. 唯一ID设置( 完成 )

   添加唯一ID生成功能，该ID仅在系统第一次运行时生成，并将该ID传输给mqtt

## 2024.12.20
1. 修改启动后默认先检测钉钉,然后必须设置邮箱(检测格式),否则就退出.（属于非必须性需求）

## 2024.12.25
1. mqtt通信异常问题（已解决：添加重连机制）
   ```
   2024-12-24 15:09:22 | MqttService | MQTT 连接断开：等待来自服务器的响应时超时
   2024-12-24 15:09:22 | MqttService | 堆栈信息：
   等待来自服务器的响应时超时 (32000)
   at org.eclipse.paho.client.mqttv3.internal.ExceptionHelper.createMqttException(ExceptionHelper.java:31)
   at org.eclipse.paho.client.mqttv3.internal.ClientState.checkForActivity(ClientState.java:747)
   at org.eclipse.paho.client.mqttv3.internal.ClientComms.checkForActivity(ClientComms.java:818)
   at info.mqtt.android.service.ping.AlarmPingSender.backgroundExecute(AlarmPingSender.kt:94)
   at info.mqtt.android.service.ping.AlarmPingSender$AlarmReceiver$onReceive$1$1$response$1.invokeSuspend(AlarmPingSender.kt:140)
   at kotlin.coroutines.jvm.internal.BaseContinuationImpl.resumeWith(ContinuationImpl.kt:33)
   at kotlinx.coroutines.DispatchedTask.run(DispatchedTask.kt:106)
   at kotlinx.coroutines.internal.LimitedDispatcher.run(LimitedDispatcher.kt:42)
   at kotlinx.coroutines.scheduling.TaskImpl.run(Tasks.kt:95)
   at kotlinx.coroutines.scheduling.CoroutineScheduler.runSafely(CoroutineScheduler.kt:570)
   at kotlinx.coroutines.scheduling.CoroutineScheduler$Worker.executeTask(CoroutineScheduler.kt:749)
   at kotlinx.coroutines.scheduling.CoroutineScheduler$Worker.runWorker(CoroutineScheduler.kt:677)
   at kotlinx.coroutines.scheduling.CoroutineScheduler$Worker.run(CoroutineScheduler.kt:664)
   ```
2. 在异常情况（类似上述）下，遗嘱消息不发送问题（未发现问题）

## 2025.1.7总结
1. mqtt通信异常问题已解决，稳定运行300小时以上。
2. 测试过程中未发现其他影响程序正常运行的BUG。
3. 未发现有必要性功能开发的需求。

### 其他非紧急BUG
1. 通知消息发送大于1次，偶发，不影响使用。
2. 在某些情况下，遗嘱消息不发送问题，偶发，不影响使用。

### 其他非必要需求
1. 启动后检测钉钉功能后,必须设置邮箱(检测格式),否则就退出：该功能主要是为了防止用户忘记添加邮箱，无法订阅mqtt等相关功能；目前可以设置完邮箱后，退出程序，重新进入即可正常使用。


## 2025.3.31
1. 需要添加tls/双向认证（紧急）-4.1已完成，正在修改小问题并记录测试
2. 修复设备密钥和CA根证书未加密的bug-已修复，4.2开始测试
3. 邮箱发送主题功能需要优化：需要应用第一次启动时强制填写邮箱，填写完成后直接发送邮件。然后由开发者重新编译带有特定证书的安装包再重新安装。--紧急
4. 需要添加网络连接状态通知功能
5. 由于设置了仅双向认证，控制端由于app不是自研，所以证书不能加密，等后面再说。

## 2025.4.14
1. 双向认证测试  --完成
2. 删除闹钟 --完成
3. 程序流程优化
   1. 程序需要修改为不带ca证书，热插入模式  --完成
   2. 初次启动后，强制填写邮箱（发送邮箱和接收邮箱）--完成
   3. 下一步强制导入CA.en和client.en  --完成
   4. 然后点击重启，重新启动程序   --不需要
   5. 再设置手机相关设置  --完成
4. 修改常见问题  --完成
5. 添加网络连接状态通知功能   --完成

## 2025.4.27
1. 代码混淆还是有必要的
2. 加壳？研究研究

## 2025.4.30
1. 解决当使用过程中证书过期后，重新下发证书但mqtt无法连接的问题。
2. 针对通知监听经常会出现两次或多次通知，以及弹窗出现多次的情况，可能是因为APP重启后自动重新绑定服务，导致 onListenerConnected() 被重复调用。
3. 针对通知监听服务不会随着程序退出而退出的问题：这种服务类型与常规的后台服务有所不同，因为它与 通知监听器（Notification Listener）相关联，并通过系统的通知机制来工作
   1. 当用户在系统设置中启用了通知监听器权限时，NotificationMonitorService 将开始工作，监听系统中的所有通知。
   2. 并不需要显式调用 startService() 来启动它。它会在满足条件时自动启动（即系统已授权通知监听权限）。
   3. 不需要手动停止这个服务。它会在系统禁用或卸载应用时自动停止。

   所以，针对通知服务出现多次，属于系统不可控行为，需要后续想办法解决  ----未解决

## 2025.5.21
1. 在网络波动，客户端没有发送心跳导致服务端断开连接后，客户端重连时一直连接不上，出现OpenSSL Error[0]: error:1417C086:SSL routines:tls_process_client_certificate:certificate verify failed
   1. 加大心跳间隔为60s，加大timeout时间为20s
   2. 设置sslContext为全局唯一单例模式，并根据证书是否重新下载来判断是否重新生成sslContext
   3. 删除connectLost回调中的重连行为（没用，因为mqtt会自动重连，如果因为证书问题，重连也会失败）

## 2025.6.4
1. 手动清空后台后，再打开，会无法正常连接监听通知服务。 --因为onListenerConnected中退出时log崩溃，导致无法重置初始化标志位，导致无法重新初始化。
2. 证书过期重新下载后/mqtt断开连接后，无法再次连接，出现：OpenSSL Error[0]: error:1417C086:SSL routines:tls_process_client_certificate:certificate verify failed。
   1. 猜测还是SSLContext没有重新初始化。是因为mqtt的自动重连是不需要重新设置的，而是直接自动重连

修改：
1. 将SSLContext设置为断开连接之后，只要断开连接，再次连接前就重新导入证书：mqtt组件要优化代码：1。解耦连接代码，要考虑ssl配置的失效问题，保证，只要断开连接，再次连接前就重新生成ssl。
2. 为了防止出现误触导致不能正常工作：添加功能：当用户点击调整亮度，屏幕锁定无法点击，必须进行解锁才可以重新点击。
3. 添加白名单电池优化。
4. 添加自启动权限。
5. 将现有的权限整合，整合为一个大的组件，也就是权限获取组件，包括悬浮窗，通知监听，白名单优化，自启动四个，缺一则不启动mqtt连接服务
6. 版本更新为3.0.0.前提是权限要正常。
7. 可以考虑添加获取钉钉打卡提醒通知，判断如果获取到通知，就模拟点击并打卡。

开发：
1. 通知监听不再由按钮控制，而是每次启动后首先检查监听权限，正常后发送一个通知，如果捕捉到说明正常，否则需要重新授权，且每次授权完成后都需要测试一下。
2. 将ca认证功能统筹规划，优化初始化逻辑：Activity + ViewModel + 协程 + 弹窗（Dialog）
3. 将权限功能和activity解耦，封装，设置为fragment调用。同时优化fragment
4. 界面上显示ID
5. 将mqtt优化整个服务

## 2025.6.9
1. 评估添加mqtt长时间连接不上后，发送邮件异常提醒
2. 评估添加通知监听服务状态UI显示
3. 评估添加通知监听服务长时间没有自启，发送邮件异常提醒
4. 评估检测钉钉下班打卡通知，模拟点击钉钉下班打卡，实现下班打卡。